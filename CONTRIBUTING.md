# Contributing

1. Keep the public API names descriptive.
2. Add comments for non-obvious Sass behavior.
3. Prefer modern module APIs.
4. Add or update an example when introducing a reusable feature.
5. Run `npm run check` before submitting changes.
6. Keep generated CSS out of source modules.
